

		<div class="col-md-4">
			<p><h2><%= product.name %></h2></p>
			<p id="url"><%= image_tag(product.image_url) %></p>
			<p><%= product.description %></p>
			<p><%= product.color %></p>
			<p><%= link_to 'Edit', edit_product_path(product) %></p>
       		<p><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>

		</div>
	
        
        
    